{
  "name": "module-03",
  "version": "1.0.0",
  "description": "- Set TS environment after installing node and nvm:\r     * `npm install -g typescript",
  "main": "index.js",
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only ./src/interface.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
